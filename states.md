# Estado Atual do Sistema

## Stack Tecnológica
- Monorepo de portal web com backend Java 17/Spring Boot 3.2.5 e frontend React 19/TypeScript/Vite.
- Backend: Spring Web, Spring Security, Spring Data JPA, Spring Data LDAP, Bean Validation, Actuator, Flyway, Flyway SQL Server, spring-dotenv, JJWT, Apache POI, BouncyCastle/Argon2, Microsoft JDBC Driver e H2 em testes.
- Frontend: React Router DOM v6, TanStack Query v5, Axios, Apache ECharts, Tailwind CSS v4, Radix UI, lucide-react, framer-motion, next-themes, Vitest e ESLint.
- Banco próprio: SQL Server `DASHBOARDS` em produção e `DASHBOARDS_DEV` em desenvolvimento.
- Fonte analítica externa: SQL Server `ETL_SISTEMA`/`esl_cloud`, consumido em leitura por views, fatos e synonyms publicados pelo ETL.
- Operação Windows por `iniciar-dev.bat`, `iniciar-prod.bat`, Maven Wrapper, scripts em `scripts/` e migrations em `database/`.

## Arquitetura e Padrões
- Navegador validado nas 12 páginas com ECharts: Coletas, Performance, Faturamento, Manifestos, Executivo, Localização, Contas a Pagar, Faturas por Cliente, Saúde ETL, Cotações (aba analítica), Integrações e Gestão à Vista. `frontend/.tmp/animation-final-audit/summary.json` consolida 50 gráficos, 64 carregamentos e 110 comparações por gráfico/cenário; candidato com uma inicialização e uma aplicação dos dados por gráfico, séries preservadas, 12 smokes de resize/tema e 25 verificações de legendas. Localização também validou filtro real da interface com API simulada. Cotações passou no cenário adicional de API rápida, eliminando a montagem prematura. APIs/fontes operacionais não foram contatadas. A melhoria visual não mede o tempo SQL de produção; os demais gargalos permanecem nas tarefas pendentes.
- Animações de gráficos corrigidas em 10/09/2026: `DashboardEChart` gerencia uma instância ECharts síncrona por montagem, sem instância temporária nem dependência do evento `finished`. Atualizações preservam a comparação profunda, os callbacks atuais e as opções de merge; `ResizeObserver` acompanha o card com um frame agendado e libera recursos na desmontagem. Todos os consumidores usam esse componente. `fast-deep-equal` 3.1.3, já existente no lockfile, é dependência direta; o adaptador anterior é usado somente para tipagem. Localização, Executivo, Contas a Pagar, Faturas por Cliente e os gráficos internos de conversão de Cotações estabilizam suas opções/paletas por dados e tema. Cotações usa `isPending` nas fontes de conversão para impedir uma montagem prematura antes de o timer iniciar a consulta. Fórmulas, fontes, alturas de cards e animações habilitadas foram preservadas.
- Validação de animações: dez testes do ciclo de vida compartilhado e três testes da página Localização cobrem atualização durante inicialização, dados equivalentes/novos, closures, eventos, tema, resize, StrictMode e chegada tardia da tabela. A execução completa `20260910-103509-123598` aprovou 687 testes backend e 265 frontend (952), TypeScript, lint, encoding, ambiente e builds. Após a correção adicional de carregamento em Cotações, o frontend final repetiu os 265 testes e todas as verificações em `.tmp/quality/20260910-animacao-final/summary.json`, com `operationalFilesPreserved: true`. Cobertura de linhas: 52,38% backend e 23,57% frontend. Evidências e limites em `docs/correcao-animacao-graficos-2026-09-10.md`.
- Auditoria das quatro páginas em 10/09/2026: `dashboardRequestQueue.ts` amplia o limite compartilhado de três GETs para Faturamento/Fretes, Manifestos, Performance e Executivo e suas dimensões específicas. Faturamento removeu a dependência do sucesso do overview e os seis timers; os hooks de leitura dos três domínios adicionais propagam cancelamento até o Axios, inclusive nas tabelas e dimensões dependentes. Exportações, downloads, autenticação e escritas continuam independentes. O benchmark agora possui fixtures e medição individual dos 19 gráficos das quatro páginas (5/7/5/2), com dados artificiais e sem acesso operacional.
- Executivo consulta somente a receita agregada de Fretes com os mesmos filtros/escopo/arredondamento do overview, evitando consultas de metas, realizado por filial, calendário e tendência que não participam do KPI executivo. Manifestos reaproveita o custo já agregado quando a janela fechada é idêntica à janela do filtro; janela parcial mantém a consulta específica. São otimizações de execução, sem alteração de fórmula, fonte, contrato JSON ou schema. Validação completa `20260910-094146-d84f1c`: 687 testes backend e 252 frontend (939), TypeScript, lint sem avisos, encoding, ambiente frontend e builds aprovados; `operationalFilesPreserved: true`. Cobertura de linhas: 52,38% backend e 21,81% frontend; 159 classes backend sem instrução executada.
- Segunda otimização em 10/09/2026: Performance inicia consultas independentes sem aguardar overview ou timers; `dashboardRequestQueue.ts` (originalmente `performanceRequestQueue.ts`) limitava a três GETs simultâneos por cliente Axios para o painel e dimensões específicas de Performance, priorizando overview/gráficos/dimensões/tabela. Auth, exportações, escritas e outros painéis ficam fora da fila. Os 11 hooks do domínio propagam AbortSignal e removem consultas obsoletas em espera; cancelamento de SQL já iniciado não foi comprovado. Em 20 carregamentos sintéticos, a mediana do primeiro desenho caiu 2.085,6→1.404,1 ms no desktop e 4.496→3.923 ms em CPU 4x/2 Mbps; Coletas ficou praticamente estável em 12 carregamentos de controle. Smoke com overview 503, larguras 1024/390 e tema escuro passou. Nenhuma regra de KPI/gráfico, polling, altura ou fonte de dados foi alterada. Evidências em `frontend/.tmp/audit2-performance`, `audit2-coletas` e `audit2-smoke`.
- Dependências frontend revisadas em 10/09/2026: Axios 1.20.0, ECharts 6.1.0, React Router DOM 6.30.6 e transitivas corrigidas dentro das restrições existentes; npm audit de runtime caiu de oito pacotes (quatro altos/quatro moderados) para dois moderados, sem altos/críticos. A atualização acrescentou cerca de 2,1% de JS comprimido às páginas medidas. No Java, as versões foram preservadas; `scripts/triage-java-advisories.mjs` expandiu os 86 avisos em 85 aliases CVE, com dez avisos críticos, 30 altos, 32 moderados e 14 baixos, ainda sem comprovação de explorabilidade. Relatórios em `.tmp/quality/audit2-npm-after.json` e `.tmp/quality/java-osv-triage.json`.
- Rodada anterior de qualidade em 10/09/2026: execução `20260910-013856-33cd19`, 678 testes backend e 237 frontend sem falhas/ignorados; JaCoCo/check, TypeScript, ESLint com zero warnings, encoding, validação do ambiente frontend e Vite aprovados. Cobertura backend: 52,04% de linhas/41,83% de ramificações, 473 classes e 161 sem instrução executada; frontend: 20,78% de linhas/statements, 69,19% de ramificações e 48,18% de funções. O resumo `.tmp/quality/20260910-013856-33cd19/summary.json` confirma `operationalFilesPreserved: true`. Candidatos em `backend/target/quality/20260910-013856-33cd19/dashboard-api-1.0.0.jar` e `frontend/.tmp/quality-build/20260910-013856-33cd19/dist`; incluem Integrações e as duas rodadas de qualidade. Nenhum artefato operacional foi substituído. Relatório atual: `docs/auditoria-aprofundada-2026-09-10.md`.
- Qualidade isolada em 10/09/2026: `scripts/run-quality.ps1` seleciona JDK 17 e executa Maven `verify`, JaCoCo, Vitest/V8, TypeScript, lint, encoding, validação do ambiente frontend e build em diretórios próprios por execução. O perfil `quality` exclui SQL Server JDBC e spring-dotenv do classpath dos testes, desabilita Flyway, limita a JVM a um fork/512 MB e não inicia o Spring Boot completo. O runner compara fingerprints de arquivos operacionais, incluindo JAR, `dist-prod`, configurações e o candidato anterior `build-check`. O perfil rejeita Java 25, causa reproduzida dos quatro erros iniciais de Mockito. Foram acrescentados testes de limites/validação de 18 DTOs, JWT/filtro, HTTP real em memória, uploads, períodos, senha, rotas/permissões, filtros, tooltips e ECharts; o `contextLoads` vazio foi removido. A medição inicial tinha 50,46% das linhas/39,80% das ramificações; a medição atual está no item anterior. A segunda rodada acrescenta a cadeia Spring Security/MVC real em memória, gestão de setores, parser JDBC, matriz ACL e integração entre hooks/QueryClient/Axios. Há pisos de cobertura explícitos, sem alegação de teste completo de todas as classes. Evidências, comandos e lacunas em `docs/qualidade-e-graficos-2026-09-10.md`.
- Gráficos em 10/09/2026: `DashboardEChart` usa `echarts-for-react/lib/core` e o registro único `echartsRuntime`, com Bar/Line/Pie, Canvas e componentes usados no portal. O wrapper geral e os quatro consumidores diretos foram alinhados. A importação modular preserva opções, eventos, tooltips, zoom, animações, temas, altura e contratos matemáticos. O fechamento de imports das 12 páginas com gráficos caiu cerca de 24,59% a 26,20%; Coletas baixou 157.345 bytes a menos de JavaScript comprimido. Em 12 carregamentos sintéticos no Edge, cinco gráficos reais renderizaram sem erro; em CPU 4x/2 Mbps, a mediana do primeiro desenho passou de 4.572,5 para 3.881,2 ms; no desktop rápido não houve ganho consistente. APIs simuladas, portas dinâmicas locais e DNS externo bloqueado; nenhum serviço operacional foi acessado. Medidores reproduzíveis em `frontend/scripts/measure-bundle.mjs` e `frontend/scripts/benchmark-charts.mjs`.
- Visualização de canhotos em 10/09/2026: a coluna de ação `Canhoto`, os botões e `CanhotoImagemModal` foram removidos de Integrações conforme autorização do usuário caso a exibição não funcionasse. A sondagem real dos três registros da captura retornou HTTP 404 no endpoint de imagem do Satélite; os dois caminhos relativos existentes, resolvidos na UI como o modal fazia, retornaram HTTP 200 com HTML de 823 bytes, não fotos. O endpoint legado extrai imagem apenas de payloads PPG e não fornece os arquivos Vedacit do SFTP. Os status Comprovante/POD e a auditoria foram preservados. A remoção foi conferida por busca de consumidores restantes, lint, 21 testes frontend existentes e build TypeScript/Vite isolado, sem teste novo que apenas espelhe a exclusão. Evidências em `docs/integracoes-canhotos-2026-09-10.md`; pacote em `frontend/.tmp/build-check`, log `frontend/.tmp/integracoes-build-sem-canhoto.log`. Nenhum runtime, imagem, SFTP ou banco foi alterado.
- Revisão de Integrações em 10/09/2026: o resumo reúne cliente, origem, resultado, conexão e horários em um cabeçalho compacto, seguido de três colunas com divisórias e indicadores alinhados. A prévia desktop mediu 131 px de altura; em tela estreita os grupos empilham, sem transbordamento da página. Zero envios usa cor neutra; contagens e tooltips foram preservados. Passaram 21 testes frontend com o adaptador real e HTTP simulado, lint e TypeScript/Vite. Build em `frontend/.tmp/build-check`, log `frontend/.tmp/integracoes-build-origem-layout.log`, prévias `frontend/.tmp/integracoes-preview/organizado.png` e `organizado-estreito.png`. Nenhum runtime ou pacote operacional foi alterado.
- Causa de origem ausente confirmada em 10/09: as duas rotas de ciclos do Satélite em execução responderam HTTP 200 sem `origemComprovantes`; o JAR operacional ainda é `EBC700C7...`. O adaptador identifica SFTP quando o campo está ausente nessas rotas exclusivas, preservando valores explícitos, inclusive `API_ESL` e `null`. Testes comprovam API ESL na coluna e no filtro quando informada, mas a consulta real ainda contém apenas auditoria do worker SFTP. Os fallbacks ESL de outros processos não são detectados por essa tabela.
- Integrações em 09/09/2026: `CiclosIntegracaoPanel` separa os arquivos reconhecidos/rejeitados na origem, as NF-es avaliadas/comprovantes enviados/pendências do ciclo e a fila normal/bloqueios/envios sem confirmação acumulados ao final. As unidades e o singular/plural ficam explícitos nos cartões e nas linhas. O dicionário de KPIs centraliza as explicações, exibidas por `TooltipKpi` ao passar o mouse ou focar pelo teclado; os próximos horários permanecem estimativas, sem afirmar que o PM2 está ligado. Falhas sem contagens disponíveis não são exibidas como inventário vazio; contagens registradas em ciclos com falha de processamento são preservadas.
- Arquitetura em camadas Spring MVC no backend e SPA React no frontend.
- Backend: `controller` expõe fronteiras HTTP; `service` orquestra regras; `repository` concentra SQL/JPA; `dto` define contratos; `model` representa entidades; `security` concentra JWT, API key e rate limit; `config` define infraestrutura; `policy` guarda regras reutilizáveis; `util` contém helpers puros.
- Repositórios SQL e testes mantêm somente constantes e imports efetivamente usados, para preservar diagnósticos Java sem avisos de código morto; a limpeza de `EtlSaudeSqlRepository` e `RetencaoHomeSolicitacoesServiceTest` foi aplicada.
- Frontend: `src/App.tsx` define rotas lazy e proteção de acesso; `contexts` guarda sessão/filtros/cabeçalho; `api` centraliza Axios e endpoints; `hooks/queries` encapsula React Query; `pages` monta dashboards; `components/shared` padroniza filtros, cards, tabelas, exportação, gráficos e estados.
- O botão de apresentação em `TopNav` abre a sequência padrão em tela cheia (`Faturamento → Cotações → Coletas → Performance → Manifestos → Indicadores de Gestão à Vista`) e filtra cada item com a mesma permissão efetiva da sessão antes de navegar. Os controles compactos ficam na própria barra superior e a troca de página não executa efeitos colaterais dentro de atualizadores de estado; `Esc`, o botão de saída, a saída nativa de tela cheia e o clique no logo para retornar à Home encerram completamente a apresentação. As rotas continuam protegidas por `RotaProtegida` como defesa de servidor/cliente.
- Durante a apresentação, `TopNav` usa uma linha compacta, porém arejada: logo, divisor, título da página, divisor, filtros, divisor, atualização e ações. A descrição fica oculta; os controles de navegação da sequência são minimalistas, alinhados e possuem área de clique confortável.
- Durante a apresentação, a barra superior recebe a `FilterBar` ativa na própria linha de navegação. A versão integrada não aplica contêiner próprio, preserva o mesmo estado dos filtros e devolve a barra à página ao encerrar a apresentação; o painel de opções abre como sobreposição abaixo da barra, usando toda a largura útil da tela sem alterar seu alinhamento ou as ações de página, e fecha ao clicar fora. A sequência troca de tela somente pelos controles clicáveis.
- O ícone de configurações ao lado do slideshow abre o gerenciador de apresentações pessoais: o usuário seleciona somente páginas que sua sessão pode acessar, define a ordem e salva no `DASHBOARDS`. O backend volta a validar a permissão efetiva antes de persistir e filtra itens revogados na leitura; a sequência padrão permanece fixa e não é gravada como versão editável.
- `clienteAxios` não fixa `Content-Type` globalmente: o Axios serializa payloads JSON quando necessário e o navegador define o `multipart/form-data` com boundary para envios via `FormData`. O `ManipuladorGlobalExcecoes` retorna `415 Unsupported Media Type` para conteúdo não aceito, em vez de mascará-lo como erro interno 500.
- Contas a Pagar expõe a série temporal por emissão, competência ou liquidação, agrupada por dia, semana ou mês. Fornecedores e centros de custo usam endpoints próprios de ranking/drill-down SQL (`raiz → classificação → despesa`), com Top 5/10/15 e métricas de valor a pagar, saldo aberto, valor pago ou títulos; esses controles são locais aos cards e não alteram os filtros globais. A Conciliação permanece no contrato e na visualização originais.
- Faturas por Cliente expõe uma série temporal local por emissão, vencimento ou baixa, agrupada por dia, semana ou mês e alternável entre valor faturado, registros e ticket médio. O Aging permite recortar todos/a vencer/em atraso e detalhar faixa para cliente e fatura/ID Único; o ranking percorre `cliente → CNPJ → fatura`, com barras verticais que aproveitam a altura útil do card, Top 5/10/15, métricas financeiras e Pareto calculados no SQL Server. Status do Processo alterna entre distribuição e evolução temporal, exibindo resumo textual quando só houver uma categoria.
- Campos preenchidos automaticamente pelo navegador preservam as cores de tema por meio da regra global de autofill; o formulário e a redefinição de senha em Administração de Usuários oferecem botão de exibir/ocultar, que alterna a senha e a confirmação juntas.
- Em Administração de Usuários, o resumo substitui o card de contas inativas por `Solicitações`: ele lista apenas usuários com pedido de redefinição pendente e oferece a ação direta para abrir a redefinição administrativa; a conclusão da redefinição já remove a solicitação no backend.
- Em comunicados da Home, apenas quem possui papel administrativo pode excluir comentários de terceiros. Usuários não administradores veem e podem excluir somente o próprio comentário; essa regra é aplicada no backend ao montar a permissão exibida e ao executar a exclusão lógica.
- Filtros globais de dashboards que usam a dimensão de filiais exibem apenas filiais próprias em `components/shared/FiliaisParceirosFilter.tsx`, reutilizando a classificação `| parceiro` centralizada em `utils/filiais.ts` para ocultar parceiros logísticos da barra operacional; a administração continua usando `FiliaisPermitidasSplitSelect` com a mesma regra segregada.
- A dimensão de filiais (`DimensoesService.listarFiliais`) normaliza os nomes para maiúsculas na leitura, tanto no cache global quanto no escopo restrito de usuário, sem hardcode de filiais específicas.
- Modais animados com múltiplos filhos diretos em `AnimatePresence` devem declarar `key` explícita e estável por elemento; o modal de justificativa de Horário de Corte identifica backdrop e diálogo pela SM selecionada para evitar chaves vazias no React/Framer Motion.
- Banco `DASHBOARDS` é gerido exclusivamente por Flyway em `database/migrations`, com baseline 22 e migrations atuais até `V067__criar_sequencias_apresentacao.sql`.
- O login usado pelo Flyway precisa de `REFERENCES` no schema `acesso` para migrations que criam chaves estrangeiras. O bootstrap administrativo e idempotente `database/setup/002_conceder_references_schema_acesso.sql` concede apenas essa permissão ao login configurado, sem ampliar permissões de dados ou do ETL.
- A migração `V063__adicionar_solicitacao_redefinicao_senha.sql` adiciona `acesso.usuarios.password_reset_requested_at`, também presente no baseline `V001`, para registrar pedidos de redefinição sem expor credenciais; os lotes DDL são separados por `GO` para que o SQL Server resolva a nova coluna antes da criação do índice filtrado.
- A migração `V064__unificar_setor_ti_admin_em_ti.sql` consolida o setor legado `TI - Admin` no setor canônico `TI`: preserva a união de permissões e filiais, move os usuários sem alterar seus papéis ou exceções individuais e inativa logicamente o setor duplicado.
- A migração `V065__restaurar_setor_administracao_e_template_ti.sql` corrige a origem do rótulo `TI - Admin`: restaura o setor de sistema `setor-admin` como `Administração`, mantém os usuários consolidados em `TI` e limita o baseline desse setor às permissões técnicas próprias; privilégios administrativos continuam sendo concedidos exclusivamente pelos papéis e exceções individuais.
- Hibernate roda com `spring.jpa.hibernate.ddl-auto=none`; DDL em runtime por Java é proibido.
- O portal é consumidor read-only do ETL. O backend deve consultar objetos analíticos por nomes simples (`dbo.vw_*`, `dbo.fato_*`, `dbo.dim_*`), sem hardcode de database.
- Padrões obrigatórios: push-down computation no SQL Server, filtros sargable, paginação/exportação em SQL, DTOs pequenos, validação explícita de período e paridade entre cálculo de KPI e dicionários do frontend.
- Faturamento e Manifestos expõem o total de dias úteis do período selecionado, calculado no SQL Server a partir de `dbo.dim_calendario`; ambas as páginas o exibem na barra principal, imediatamente após o intervalo compacto de datas, separado por divisor vertical e com a mesma tipografia dos valores do intervalo.
- Grids de gráficos dos dashboards preservam o layout amplo a partir de `2xl` (`1536px+`) e aplicam fluxo contínuo somente nas larguras intermediárias. A partir de `lg` (`1024px`), gráficos da mesma hierarquia são distribuídos em duas colunas sem estiramento artificial; quando a quantidade de cartões exige um elemento isolado, o primeiro gráfico naturalmente largo ocupa a linha inteira e os demais formam pares. Em `2xl+`, páginas com composição original específica usam malhas organizadas (`2xl:grid-cols-6` ou `2xl:grid-cols-12`) e spans fixos para recuperar linhas 3/2, 6/6 ou 5/3/4. Apenas gráficos naturalmente largos nas larguras intermediárias usam `col-span-full`, sempre com override `2xl:*` quando não devem permanecer full width em monitores grandes. Coletas, Manifestos, Faturamento, Tracking, Faturas por Cliente, Contas a Pagar, Cotações, Performance, Executivo, Integrações e Saúde do ETL aplicam essa malha intermediária.
- Em Faturamento, o gráfico de evolução e a tabela Top 10 Clientes ocupam cada qual uma linha integral no estágio `lg`; Classificação e Responsável formam um par, assim como Participação de Clientes e Faturamento por Rota. Os controles visuais de rolagem do menu lateral são botões funcionais e acessíveis, que deslocam a navegação do drawer sem propagar o clique para o conteúdo atrás dele.
- Os oito KPIs de Faturamento usam quatro colunas e duas linhas equilibradas entre `lg` e `xl`; em `2xl`, passam para uma única linha com colunas proporcionais: Minutas compacta, Faturamento Realizado/Líquido e Meta de Faturamento amplos, Ticket/Peso/Volumes compactos e Tendência intermediária.
- Os KPIs de Faturamento preservam a altura natural dos cards de Coletas; Meta e Tendência usam conteúdo compacto e reorganizado: valor no corpo e, no rodapé, texto auxiliar truncável ao lado da barra de progresso quando aplicável, sem elevar a linha inteira.
- No card "Taxas de Conversão" de Cotações, o grid interno muda para duas colunas a partir de `lg`, mantendo a evolução à esquerda e os resumos LTL/FTL/PTL à direita em desktops; os contêineres ECharts preservam altura mínima de `100px`.
- Os KPIs de Localização de Cargas usam uma malha responsiva de 2/3/6 colunas (`base`/`md`/`lg`), com "Val. Carteira" ocupando dois spans para manter os cinco indicadores em uma única linha no estágio intermediário e dar espaço ao valor monetário.
- Os cinco KPIs de resumo de Indicadores de Gestão à Vista ficam lado a lado em uma malha de cinco colunas a partir de `lg`.
- Os quatro KPIs superiores de Integrações ficam em uma única linha de quatro colunas a partir de `lg`.
- Os cards-resumo de Indicadores de Gestão à Vista preservam o espaçamento interno e usam slots de altura fixa para título, descrição e detalhe; assim, valores, status, metas e barras de progresso ficam alinhados mesmo quando os textos quebram em quantidades diferentes de linhas.
- O Panorama Operacional não reserva altura mínima e, a partir de `lg`, organiza título, valor, status e progresso de cada indicador na mesma linha, reduzindo a altura dos cinco cartões sem perder informação.
- Na Localização de Cargas, "Val. Carteira" é o quinto KPI e ocupa a linha inteira em mobile; o cabeçalho da matriz "Responsável pela Região de Destino" permite quebra dos filtros rápidos para evitar sobreposição horizontal.
- Na matriz "Responsável pela Região de Destino", os indicadores móveis são centralizados em duas colunas e "Fora do Prazo" ocupa a linha inteira, eliminando o indicador órfão e preservando o alinhamento à direita a partir de `md`.
- Cada indicador da matriz de regiões recebe bloco visual compacto em mobile (borda, fundo e espaçamento próprios), enquanto o layout tabular sem caixas é restaurado em `md+`.
- O gráfico "Top Clientes por Valor Faturado" resume o eixo Y em até 24 caracteres, sem CNPJ, mas mantém o rótulo completo como categoria para o tooltip; a grade reserva apenas 112px para o eixo, liberando largura para as barras.
- Os gráficos principais de Saúde do ETL ficam empilhados até `2xl`; apenas no layout amplo entram em duas colunas, quando o card de sucessos também consegue posicionar seus KPIs lateralmente sem criar altura vazia na coluna vizinha.
- A partir de `sm` (640px), os quatro KPIs de "Sucessos/Falhas por Dia" ficam em uma única faixa abaixo do gráfico, sem altura mínima ociosa; apenas próximo de celular permanecem em duas colunas e, no layout amplo (`2xl`), voltam à coluna lateral.
- Os cinco KPIs superiores de Saúde do ETL usam duas colunas até `lg`, com "Taxa Sucesso" ocupando integralmente a última linha; em `lg+`, os cinco ficam na mesma linha, sem uma faixa intermediária de quatro colunas.
- Os KPIs de Manifestos - Performance de Veículos usam seis colunas de `lg` a `xl`: os quatro status e KM Total (com dois spans) fecham a primeira linha; Custo Total (dois spans) e os quatro indicadores financeiros fecham a segunda, sem cards comprimidos em uma faixa de 11 colunas. Em duas colunas, KM Total ocupa integralmente sua linha e Custo Total completa a próxima ao lado dele, sem indicador órfão.
- Os KPIs da página Performance usam seis colunas de `lg` a `xl`: os seis indicadores operacionais ocupam a primeira linha e Peso Taxado, Comprovante Anexado e Valor NF sem Comprovante (dois spans cada) ocupam a segunda; em mobile o último ocupa a largura completa para não ficar órfão.
- Em `2xl`, os KPIs de Manifestos e Performance deixam de ocupar duas linhas dentro de uma malha de doze colunas. Manifestos mantém KM Total e Custo Total em dois spans, enquanto Performance mantém Peso Taxado, Comprovante Anexado e Valor NF sem Comprovante em dois spans; os demais KPIs ocupam um span. Os layouts mobile e intermediário até `xl` preservam suas grades e spans anteriores.
- O seletor de visões de Cotações é uma grade fixa de quatro opções sem rolagem horizontal; antes de `lg`, usa rótulos compactos (Usuário, Filial, Clientes e Análise), preservando os títulos completos no desktop.
- Os sete KPIs do Executivo não deixam card órfão: Ocupação de Manifestos ocupa a faixa final em duas colunas, fecha a linha `md` com dois spans e, em `lg`, divide a segunda linha com Previsão Vencida; em `xl+`, os sete voltam a um único alinhamento.
- `ChartWrapper` aplica altura mínima de 350px ao canvas do ECharts e os estados de loading, vazio e erro em `ChartCard` preservam o mesmo respiro mínimo; cartões customizados de gráficos devem manter altura física compatível com esse mínimo.
- Produção e desenvolvimento são isolados por `.env`, profiles e validações que impedem DEV de conectar no banco `DASHBOARDS`.
- Exportações CSV baseadas em records Java usam `CsvExportWriter`; quando um DTO precisa de cabeçalho amigável sem alterar o contrato JSON, o componente pode usar `@CsvColumn`, como em `HorarioCorteRowDTO.justificativa` exportado como `Justificativa`.
- As metas de custo operacional de Manifestos usam `ManifestosCostGoalService` como fronteira canônica de escrita manual/replicação e `ManifestosMetasImportacaoExcelParser` como fronteira de upload; filial passa pela sigla operacional curta via `FilialKeyUtils`, tipo de contrato e classificação são normalizados por `trim().toUpperCase()` antes de persistir, com a migração `V051__normalizar_chaves_metas_manifestos.sql` corrigindo dados legados no banco próprio.
- A gestão de metas de Manifestos em `ManifestosCostGoalsPanel.tsx` separa filtros de topo por Mês/Ano e Filial, usa esses filtros para o formulário de edição e renderiza os orçamentos cadastrados em tabela relacional por contrato, classificação, meta mensal formatada em moeda e última atualização. O seletor de filial exibe a descrição da dimensão como label, mas usa sempre a sigla operacional curta como `value`, filtra parceiros logísticos e seleciona a primeira filial válida disponível apenas enquanto o usuário ainda não escolheu uma filial. A listagem de metas é consultada por `branchId` curto, `ano` e `mes`, com `queryKey` isolada no formato `['manifestosMetas', filial, mes, ano]` e sem reaproveitar dados de outra filial quando a resposta filtrada vem vazia. As linhas da tabela usam o `id` persistido da meta como `key` React.
- A whitelist de clientes dispensados de cubagem é estado da aplicação em `dbo.cliente_excecao_cubagem`, criada por Flyway com CNPJ limpo `VARCHAR(14)`, `CHECK` numérico, índice único `UX_cliente_excecao_cubagem_cliente_cnpj`, flag `ativo` para exclusão lógica, índice `IX_cliente_excecao_cubagem_ativo_razao_social` e seed dos CNPJs legados que antes vinham do serviço Java. O importador usa `CubagemClientesExcecaoImportacaoParser` para XLS/XLSX/CSV com assinatura validada, `CubagemClientesExcecaoImportacaoService` para preview/resultado, `UsuarioAutenticadoContextService.getNomeComPapel()` para auditoria e `ClienteExcecaoCubagemSqlRepository` para `MERGE` idempotente que reativa CNPJs inativados.
- A observabilidade de sessões administrativas usa as colunas `ultima_atividade` (`DATETIMEOFFSET`) e `ultima_rota_acessada` (`VARCHAR(100)`) em `acesso.usuarios`, adicionadas por Flyway, reforçadas pela migration idempotente `V055` e refletidas no baseline `V001`; o índice filtrado `IX_usuarios_ultima_atividade` suporta o cálculo de usuários online.
- `FiltroValidacaoJwt` registra heartbeat de requisições autenticadas via `UsuarioSessaoSqlRepository`, usando `SYSDATETIMEOFFSET()` no SQL Server e a rota SPA enviada pelo frontend no header `X-Dashboard-Route`, com fallback para o path HTTP da API.
- A flag `acesso.usuarios.exige_troca_senha` já integra o baseline `V001`. O reset administrativo usa Argon2id para a senha temporária, revoga todos os refresh tokens do usuário e impede que tokens JWT ou refresh tokens anteriores autentiquem uma conta enquanto a troca obrigatória estiver ativa.
- A geração de sessão recarrega o usuário ativo pela identificação antes de montar o DTO. Isso mantém `setor` acessível no fluxo de login após a entidade original ter sido desanexada e impede que uma `LazyInitializationException` seja convertida em HTTP 503.
- `CorsConfig` permite explicitamente o header `X-Dashboard-Route` nos preflights de `/api/**`, tanto via `WebMvcConfigurer` quanto via `CorsConfigurationSource` usado pelo Spring Security, preservando o heartbeat de rota em DEV/PROD sem bloquear requisições autenticadas no navegador.
- Dados de negócio, justificativas e auditoria usam exclusão lógica obrigatória; hard delete/`DELETE FROM` físico é proibido. Entidades JPA podem delegar o soft delete ao Hibernate com `@SQLDelete`/`@SQLRestriction`, e queries JDBC nativas devem filtrar registros inativos explicitamente.
- A série gráfica de Performance de Entrega em Gestão à Vista usa o enum backend `NivelVisaoPerformance` (`RESPONSAVEL`, `REGIAO`, `CIDADE`) como contrato estrito de visão. O DTO expõe `label`, `filtro`, `visao`, totais físicos e percentual; o SQL seleciona e agrupa apenas colunas mapeadas pelo enum.
- Componentes de seção de Gestão à Vista podem repassar `chartActions` e `chartEvents` ao `ChartWrapper`, permitindo breadcrumbs e drill-down em Apache ECharts sem duplicar wrappers visuais.
- `ChartWrapper` tipa e encaminha `onEvents` diretamente ao `echarts-for-react`; dashboards com drill-down devem passar handlers de clique pelo wrapper, usar a seta do breadcrumb para retornar à raiz e deixar níveis indisponíveis com `disabled`/`cursor-not-allowed`.
- O painel de Coletas consome a coluna `[Região Logística]` publicada por `dbo.vw_coletas_powerbi` para o gráfico e filtros de origem. A região bruta `[Região da Coleta]` permanece no contrato como dado de compatibilidade, enquanto `ColetasRegiaoOrigemDTO.regiaoLogistica` e `ColetaResumoDTO.regiaoLogistica` expõem a macro-região governada.
- A tabela analítica de Horário de Corte em Gestão à Vista usa `AnalyticalDataTable` com filtros de coluna para Filial, Linha/Operação e Status. Esses filtros são serializados por `useAnalyticalTableFilters`/`TableApiFilters` como `f.tabelaBusca` e `f.tabelaColuna.*`, sem filtragem client-side sobre páginas parciais.
- `HorariosCorteRasterSqlRepository` aplica escopo de filiais, filtro global da tabela e filtros de coluna em `WHERE` parametrizado antes de `COUNT_BIG`, `ORDER BY` e `OFFSET/FETCH`, mantendo paginação, totalização e exportação CSV alinhadas.
- A página de Saúde do ETL usa o filtro global `DateRangePicker`/`useFiltro`, inicializa a janela em 180 dias e mantém os cards integrados ao `TooltipKpi` com definições centralizadas em `kpiDictionary.ts`.
- A tabela "Auditoria crua de extrações" em Saúde do ETL mantém a coluna `Mensagem` compacta em uma linha, normalizando quebras de linha para espaços e preservando o texto original no `title` da célula.
- O card "Sucessos/Falhas por Dia" de Saúde do ETL usa layout interno em grid no desktop: gráfico de barras empilhadas à esquerda e KPIs reativos do período à direita, consumindo a série diária agregada no SQL Server a partir de `dbo.log_extracoes.timestamp_inicio` e `status_final`.
- O gráfico "Evolução de Inserções e Atualizações" de Saúde do ETL consome um contrato próprio com `dataReferencia`, `insercoes` e `atualizacoes`; a agregação diária é feita no SQL Server por `GROUP BY CAST(timestamp_inicio AS DATE)` sobre `dbo.log_extracoes`.
- O gráfico "Resumo por Tabela Alvo" em Saúde do ETL ocupa largura total abaixo dos dois gráficos diários, usa barras horizontais em ECharts ordenadas por volume crescente, altura fixa de 400px e `dataZoom` interno/slider no eixo Y para navegar por muitas tabelas sem esticar a página. O gráfico destaca os nomes das tabelas no eixo vertical, exibe no tooltip quantidade de extrações, sucessos, falhas e janela temporal de processamento por entidade/tabela alvo, e mantém KPIs internos compactos ao lado do gráfico em `xl+`, com "Dados desde" e "Dados até" no topo do bloco antes de tabelas monitoradas, extrações, sucessos, falhas, registros gravados, intervalo, primeira/última extração, e com "Maior Volume" e "Tabelas com Erro" pareados na mesma linha; quando um detalhe de status ainda não vem no contrato, o frontend usa fallback seguro e nunca renderiza `NaN`.
- Os KPIs internos "Primeira" e "Última" do resumo por tabela alvo em Saúde do ETL exibem tooltip contextual por hover/foco, esclarecendo que representam a primeira/última extração registrada na base histórica do período filtrado, e não o instante de execução do ETL.
- O resumo por tabela alvo em Saúde do ETL estende o DTO `EtlTabelaAuditoriaResumoDTO` com `menorDataNegocio` e `maiorDataNegocio`, calculados no SQL Server por `MIN/MAX` sobre as views analíticas publicadas (`dbo.vw_*_powerbi`) e exibidos no topo do bloco como "Dados desde" e "Dados até".
- Os KPIs superiores de Integrações usam `KpiCard` com fundo neutro padrão do dashboard; tons operacionais de sucesso, alerta e erro são aplicados exclusivamente ao valor numérico por classes `text-positive`, `text-warning` e `text-negative`, seguindo o padrão visual de ETL Saúde.
- Em Saúde do ETL, o KPI `Com Erro` só usa verde quando não há falhas; qualquer execução com erro fica em amarelo até 2% do total e em vermelho acima desse limite.
- A página de Integrações separa a navegação hierárquica: o topo alterna apenas entre `Integrações` e `Quarentena`, enquanto os filtros `Pendências Operacionais` e `Integrados com Sucesso` ficam anexados imediatamente acima da tabela analítica. Os gráficos de Sazonalidade, Saúde por Sistema Destino e Resumo por Entidade de Integração usam a mesma quebra `grid-cols-1`/`xl:grid-cols-2`, espaçamento e altura controlada dos gráficos de ETL Saúde.
- A página de Integrações exibe o gráfico ECharts horizontal "Resumo por Entidade de Integração" abaixo dos dois gráficos superiores, inspirado no bloco "Resumo por Tabela Alvo" de Saúde do ETL: barras horizontais de volume total por etapa operacional (`sistema_destino - XML/Dados` e `sistema_destino - Canhoto`) e KPIs laterais de etapas, processado, sucessos, erros/pendências, taxa de sucesso, etapas com erro, maior volume e etapa crítica. O bloco deriva os dados das métricas consolidadas já retornadas por `/api/painel/integracoes`, não chama `/api/painel/integracoes/resumo-tabelas`, e usa `dataZoom` interno/slider no eixo Y quando houver muitas entidades.
- O card administrativo "Usuários Online Agora" usa popover por hover/foco aberto abaixo do card para listar os usuários online carregados diretamente em `/api/admin/acesso/usuarios/resumo-sessoes`, com nome, e-mail e tempo amigável desde `ultimaAtividade` calculado no frontend; ele não depende da listagem completa de usuários para revelar quem está online.

## Fluxo de Dados e Integrações
- Medição individual dos 19 gráficos em 60 carregamentos consolidados e quatro smokes: Faturamento terminou os desenhos 17,3% antes no desktop e 12,7% antes em CPU 4x/2 Mbps. Executivo reduziu seis para três GETs de dados ao aplicar o preset inicial; terminou 30,2% antes no desktop, mas 2,7% depois no cenário limitado. Manifestos teve -8,3%/+4,4% de tempo e Performance -2,9%/-0,8%. Esses são resultados sintéticos, sem custo SQL, e não comprovam aceleração universal ou de produção. Smokes das quatro páginas passaram em 1024/390 px e tema escuro; Faturamento/Performance/Executivo renderizaram gráficos com overview 503. Evidências, tempos por gráfico e limites estão em `docs/performance-quatro-paginas-2026-09-10.md`.
- O resumo e histórico de comprovantes exibem `origemComprovantes`, com cliente e origem filtrando os cartões e o histórico; o resultado filtra somente as execuções históricas. Nas duas rotas exclusivas de ciclos SFTP, campo ausente em resposta antiga é identificado como SFTP pelo contrato da consulta, sem usar nome de cliente nem alterar contagens/paginação. Origem explicitamente desconhecida permanece `Não informada`. `API_ESL` explícita é preservada e exibida como API ESL. O parâmetro `origem` percorre as duas APIs até a consulta/contagem do Satélite; filtros reiniciam a página e não reutilizam linhas anteriores. Filtro ignorado mostra aviso e não fabrica paginação local. A auditoria atual pertence exclusivamente ao processo SFTP; falta registrar ciclos e origem efetiva dos outros processos para monitorar seus fallbacks ESL aqui, sem inferência por ausência de consumo.
- Fluxo web: React -> hooks React Query -> `src/api/endpoints` -> `clienteAxios` -> controllers Spring -> services -> repositories SQL/JPA -> SQL Server -> DTOs -> cards/gráficos/tabelas.
- Os filtros globais de filiais trafegam como `f.filiais` e `f.parceirosLogisticos`; `FiltroRequestMapper` consolida os parceiros em `filiais` antes de construir `FiltroConsultaDTO`, preservando os services/repositories existentes sobre a mesma dimensão analítica de filial. Manifestos aceita a filial global tanto como label longo (`AGU - ...`) quanto como sigla (`AGU`); `ManifestosPerformanceService` preserva o filtro original para a fato e cria uma cópia orçamentária com sigla curta via `FilialKeyUtils` antes de chamar metas.
- `FiltroContext` suporta `setFiltros` para atualizar múltiplos filtros da URL de forma atômica, usado por atalhos de metas que precisam alternar entre filial própria, parceiro logístico e escopo global sem deixar parâmetros órfãos.
- Autenticação: `/api/auth/login`, `/api/auth/nova-senha-obrigatoria`, `/api/auth/me`, `/api/auth/alterar-senha`, `/api/auth/refresh` e `/api/auth/logout`. Login com `exige_troca_senha = true` retorna `428` e `{"status":"PASSWORD_RESET_REQUIRED","userId":"..."}` sem JWT nem cookie de refresh; a troca obrigatória valida a senha temporária e, ao concluir, emite a sessão normal. A `LoginPage` deriva a troca pendente diretamente de `location.state` na rota `/auth/trocar-senha`, evitando estado local inicializado uma única vez quando o React reutiliza o componente entre as rotas.
- Dashboards principais: `/api/painel/coletas`, `/manifestos`, `/fretes`, `/tracking`, `/performance`, `/faturas-por-cliente`, `/contas-a-pagar`, `/cotacoes`, `/executivo`, `/etl-saude`, `/integracoes` e `/indicadores-gestao-a-vista`.
- Endpoints de apoio: `/api/dimensoes`, `/api/admin/acesso`, `/api/admin/acesso/usuarios/resumo-sessoes`, `/api/admin/acesso/usuarios/importacao`, reset administrativo `POST /api/usuarios/{usuarioId}/reset-senha`, `/api/kpi-goals`, `/api/etl/quarentena`, paginação `/api/painel/*/tabela/paginada`, exportação `/api/painel/*/exportacao`, gestão de clientes sem cubagem em `/api/painel/gestao-vista/cubagem/clientes`, importação em `/api/painel/gestao-vista/cubagem/clientes/importacao` e justificativas de Horário de Corte em `/api/painel/indicadores-gestao-a-vista/horarios-corte/justificativas`.
- Frontend possui rotas protegidas para Coletas, Manifestos, Faturamento/Fretes, Performance, Tracking, Faturas por Cliente, Contas a Pagar, Cotações, Indicadores de Gestão à Vista, Executivo, ETL Saúde, Integrações e Administração.
- Objetos analíticos lidos do ETL incluem `dbo.vw_coletas_powerbi`, `dbo.vw_fretes_powerbi`, `dbo.vw_manifestos_powerbi`, `dbo.vw_localizacao_cargas_powerbi`, `dbo.vw_contas_a_pagar_powerbi`, `dbo.vw_cotacoes_powerbi`, `dbo.vw_sinistros_powerbi`, `dbo.vw_fato_manifestos_dash`, `dbo.fato_fretes_faturamento`, `dbo.fato_gestao_vista_fretes`, `dbo.fato_gestao_vista_coletores`, `dbo.fato_gestao_vista_faturas`, `dbo.fato_gestao_vista_manifestos`, `dbo.dim_calendario` e `dbo.vw_dim_*`.
- Coletas: o ETL normaliza status, respeita `status_updated_at_em` para impedir regressão temporal e executa reconciliação histórica independente da data da solicitação. Em 29/07/2026, a primeira leitura completa de 90 dias (29/04 a 28/07) sincronizou 14.578 registros retornados pelo ESL e registrou 126 ausências com confirmação `1/2`; nenhuma coleta foi excluída. A `vw_coletas_powerbi` mantém a coleta ausente visível como `Excluída`: na confirmação `1/2`, a flag `[Excluída na Origem]` ainda é `0`; na segunda confirmação, `excluido_na_origem` e a flag passam a `1`, sem exclusão física. Uma chave que reaparece é reativada. Tabela e exportação preservam esse histórico; KPIs, gráficos, totais e rankings filtram o status `Excluída` no SQL Server antes da agregação. A view é granular por coleta: quando houver vários manifestos ativos, ela publica somente o de maior `sequence_code`, impedindo que um `JOIN` multiplique indicadores ou linhas da tabela. O Dashboard chega à view pelo synonym e sem cache HTTP; o React Query mantém dados por até cinco minutos e faz nova consulta em segundo plano a cada 30 minutos com jitter, portanto um recarregamento da página consulta novamente a fonte.
- A região logística de Coletas é resolvida no ETL por `dbo.dim_regiao_logistica_rules` e publicada em `dbo.vw_coletas_powerbi.[Região Logística]`; o Dashboard apenas consome essa coluna por nome simples, sem hardcode de database.
- Estado próprio em `DASHBOARDS`: schema `acesso` para usuários, papéis, permissões, setores, filiais permitidas, refresh tokens, audit logs, metas de KPI, metas de fretes, metas de custo de manifestos, comunicados e solicitações de melhoria da Home; `dbo.viagem_justificativas` para justificativas de viagens Raster usadas pelo indicador de Horário de Corte; `dbo.cliente_excecao_cubagem` para CNPJs ativos/inativos de pagadores corporativos dispensados do indicador de Cubagem de Mercadorias, com auditoria em `atualizado_por` e `data_atualizacao`.
- Administração de usuários: `GET /api/admin/acesso/usuarios` retorna `isOnline`, `ultimaAtividade` e `ultimaRotaAcessada` já formatada para nome de negócio quando possível, com fallback seguro quando `ultima_rota_acessada` ainda não existe em um banco antigo; `GET /api/admin/acesso/usuarios/resumo-sessoes` agrega `totalUsuarios`, `usuariosAtivos`, `usuariosInativos`, `usuariosOnline` e `usuariosOnlineDetalhes` no SQL Server. O frontend consome ambos por React Query, mostra erro real na tabela quando a listagem falha e invalida lista e resumo de forma coordenada após criação, edição, inativação ou importação de usuários.
- Metas de custo de Manifestos entram por cadastro manual, replicação mensal ou importação Excel/CSV; todos os caminhos passam por normalização canônica em caixa alta antes do `saveAndFlush`, e a agregação de orçamento em `ManifestosCostGoalRepository` compara `branch_id`, `contract_type_key` e `classification_key` normalizadas para que filtros como `AGU - ...`/`AGU` e classificações como `distribuição`/`DISTRIBUIÇÃO` resolvam a mesma meta. O endpoint `GET /api/painel/manifestos/metas` aceita `branchId`, `ano` e `mes`; quando `branchId` é informado, o serviço faz push-down estrito por filial e competência no repositório e retorna `[]` quando não houver meta para a filial selecionada, sem fallback global. O DTO de listagem expõe o `id` da entidade para keys estáveis no React. No frontend, a filial selecionada no painel de orçamento é a sigla curta derivada da dimensão, evitando miss entre labels longas como `AGU - ...` e `branch_id = AGU`; na Performance de Manifestos, `ManifestosPerformanceService` passa o filtro original para fatos e um filtro clonado com `filiais` normalizadas para orçamento, enquanto `ManifestosPerformanceSqlRepository`, `VisaoManifestosRepository` e `DashboardExportSqlBuilder` filtram por `filial_key` ou expressão SQL equivalente extraída da fato. O preview de importação de metas cria chaves sintéticas explícitas para linhas e erros antes de entregar os dados ao `DataTable`.
- Integração adicional: `IntegracaoSateliteClient` aponta para `APP_INTEGRATION_SATELITE_URL` com padrão `http://127.0.0.1:19090`.
- Builds: dev usa backend 5011 e frontend 5174; produção usa backend 5010 e frontend 5173, com Cloudflare Tunnel previsto. O deploy operacional pelo PM2 usa `pm2 startOrRestart ecosystem.config.js --update-env`, que registra processos ausentes e reinicia em conjunto `Dashboard-API-5010` e `Dashboard-UI-5173` com o ambiente atualizado.
- Gestão à Vista: `/api/painel/indicadores-gestao-a-vista/performance-entrega/serie` exige `visao` e aceita `responsavelFiltro`/`regiaoFiltro` para drill-down. O frontend envia esses parâmetros por `indicadoresGestaoAVistaServico.ts`, inclui todos no `queryKey` do TanStack Query e atualiza o gráfico ao clicar em barras.
- Gestão à Vista/Cubagem: a página `IndicadoresGestaoAVistaPage.tsx` integra `CubagemClientesExcecaoPanel` dentro da seção de Cubagem de Mercadorias. O painel inicia minimizado, mostra resumo de total ativo, última atualização e exemplos de clientes em uma linha truncada com reticências, lista CNPJ, Razão Social, Última Atualização e Ações quando expandido a partir de `GET /api/painel/gestao-vista/cubagem/clientes`, abre `CubagemClientesImportacaoModal` pelo botão `Importar clientes sem cubagem` e remove exceções por `DELETE /api/painel/gestao-vista/cubagem/clientes/{cnpj}`. Upload e exclusão invalidam `['indicadores-gestao-a-vista', 'cubagem-clientes-excecao']` e `['indicadores-gestao-a-vista', 'cubagem-mercadorias']`; o modal mantém preview em `DataTable`, sumário de "Total de linhas", "Válidas para importação" e "Com problemas" e resultado final.
- A rota `/performance` mantém drill-down por `drillNivel`, `drillResponsavel` e `drillRegiao` nos query params; esses valores entram no `queryKey` de `usePerformanceDrilldown`, nos filtros da tabela e nos handlers de clique do gráfico.
- A Home concentra o catálogo em cards compactos no painel principal; o contêiner da página preserva sombras externas, sem recorte horizontal, e comunicações ocupam uma sidebar ampliada para leitura confortável. No estágio intermediário (`lg`), a coluna principal mantém solicitação de melhoria e catálogo em sequência, enquanto o Command Center fica ao lado com largura de 320px e se estende até o fim do catálogo; sua lista interna ocupa a altura disponível e rola sem aumentar a página. No estágio máximo (`2xl`), `HomePage.tsx` usa uma grade de duas colunas e duas linhas: solicitação e catálogo ocupam a primeira coluna, enquanto o Command Center de 380px ocupa ambas as linhas. Sua borda inferior fica alinhada à última linha do catálogo (como Cotações), e a lista interna rola para não aumentar a página. O Command Center recebe borda, sombra, ícone, abas e ações azuis, preservando fundo sólido neutro; sua lista de comunicados tem rolagem automática contínua quando há conteúdo excedente, sem botão de expansão, pausando ao passar o cursor ou navegar por teclado sobre a lista. O resumo de indicadores integra o card de boas-vindas em faixa compacta, reduzindo significativamente sua altura e o espaço entre seções; seus quatro indicadores ficam em uma linha de quatro colunas a partir de `sm`. A partir de `lg`, saudação, busca e resumo de acesso compartilham uma única linha, com a busca reduzida de forma fluida e os chips de acesso mantidos em uma malha fixa de duas colunas, evitando um chip isolado na segunda linha. A solicitação de melhoria é um bloco visual destacado em verde no conteúdo principal, com formulário organizado e tipo, necessidade, contexto, resultado esperado e local de aplicação opcionais visíveis; em `2xl`, sua área de contexto usa a altura do formulário para ampliar a leitura e levar o atalho de gerenciamento para o rodapé do card. A saudação é compacta e usa o nome da pessoa, sem repetir o título da rota. Qualquer usuário autenticado pode criar uma solicitação em `/api/painel/home/solicitacoes`, identificada no servidor pelo usuário da sessão, e consultar somente as próprias solicitações ativas em `/api/painel/home/solicitacoes/minhas`. O modal separa Pendente, Concluídas e Arquivadas em botões com suas contagens. Concluídas ficam disponíveis por 60 dias e então passam automaticamente a Arquivadas; arquivamento manual segue o mesmo destino. Após mais 60 dias, a solicitação é ocultada por exclusão lógica e seus anexos já são purgados, preservando somente a auditoria mínima. Cada solicitação aceita até cinco anexos PDF, PNG/JPG, XLS/XLSX ou CSV (10 MB por arquivo e 20 MB no total), com assinatura ou conteúdo textual validado no servidor; eles ficam disponíveis apenas ao solicitante e a quem administra as solicitações. Na lista, arquivos CSV e planilhas recebem ícone próprio e todos os anexos são baixados com o nome original. Quem possui a permissão de gerenciar comunicações abre a lista administrativa pelo mesmo atalho, pode marcá-las como concluídas e arquivá-las. A retenção é executada diariamente pelo backend, com prazos configuráveis em `home.solicitacoes.retencao.concluidas-dias` e `home.solicitacoes.retencao.arquivadas-dias`.
- Os cards de comunicado do Command Center mantêm o resumo compacto e expandem a mensagem inteira apenas por ação intencional no card; o atalho `Ver mais` aparece imediatamente acima do texto e sinaliza o estado com chevron, sem expandir ao aproximar o mouse dos botões de interação. Cada comunicado persistido pode receber uma curtida por usuário; `acesso.home_comunicado_curtidas` preserva o histórico por exclusão lógica, expõe contagem e nomes de quem curtiu no `GET /api/painel/home/comunicados` e alterna a curtida do usuário autenticado em `POST /api/painel/home/comunicados/{id}/curtidas`. A contagem e a lista nominal são agregadas no SQL Server, e o card revela os nomes por hover ou foco no botão de coração. O retorno também agrega em SQL Server os comentários ativos de cada comunicado, exibindo a quantidade ao lado do ícone de comentário e atualizando-a após envio ou exclusão. O modal de criação/edição usa camada fixa `z-[100]`, acima da Home e da sidebar.
- Comentários de comunicados são persistidos em `acesso.home_comunicado_comentarios` com autoria, data e exclusão lógica. O card abre a conversa sob demanda pelo botão `Comentar`, carrega os comentários ativos em `GET /api/painel/home/comunicados/{id}/comentarios` e qualquer usuário autenticado publica uma nova mensagem validada de até 700 caracteres em `POST /api/painel/home/comunicados/{id}/comentarios`.
- Comentários de comunicados expõem o controle visual `×` somente para quem pode excluí-los: o autor pode inativar apenas o próprio comentário, enquanto somente administradores podem inativar comentários de qualquer usuário. O servidor aplica a autorização em `DELETE /api/painel/home/comunicados/{id}/comentarios/{comentarioId}` e preserva o registro para auditoria com `ativo = 0`.
- O botão de curtida do Command Center recebe um duplo pulso fluido (`tum-tum`) no próprio ícone do coração, em ciclos de 12 a 15,6 segundos e atrasos desencontrados por card, para sugerir interação sem competir com o conteúdo. Ao adicionar uma curtida, apenas o card acionado dispara uma breve explosão de corações; ambos os efeitos respeitam `prefers-reduced-motion`.
- Ao hover ou foco nos botões de interação dos comunicados, `Curtir` solta pequenos corações vermelhos ascendentes e `Comentar` exibe um balão azul lateral de “digitando…” com três pontos sequenciais. A microinteração é somente visual, não altera o estado nem abre o comunicado.
- O ícone de lâmpada da solicitação de melhoria chama atenção desde a abertura da Home como uma ideia acendendo: oito riscos brancos surgem simultaneamente ao redor do ícone, com halo próximo e uma elevação quase imperceptível, sem repetir o pulso de curtida. Por solicitação explícita de produto, esse chamado permanece ativo mesmo quando o sistema sinaliza redução de movimento; elementos que precisarem respeitar essa preferência devem usar o modificador `home-request-idea-icon--respect-reduced-motion`.
- Na Home a partir de `lg`, o Command Center é posicionado fora do fluxo vertical da coluna principal e ocupa exatamente sua altura, com a borda inferior alinhada ao fim do catálogo de dashboards. A lista de comunicados usa rolagem interna para conteúdo excedente, sem criar espaço vazio entre a solicitação e o catálogo.
- A navegação lateral separa `Principal`, `Dashboards` e `Administração`: o grupo de dashboards visíveis é ordenado alfabeticamente por rótulo e exclui `Executivo` e `ETL Saúde`; ambos permanecem acessíveis pelas permissões próprias, mas são exibidos no grupo administrativo junto dos itens de gestão de acesso. A regra é centralizada em `isAdministrativeDashboardNavItem`, aplicada tanto ao drawer responsivo de `TopNav.tsx` quanto à `BarraLateral.tsx`.
- Na solicitação de melhoria, a partir de `md`, Contexto e impacto ocupa a coluna esquerda por duas linhas, Resultado esperado e Onde será aplicado ocupam a direita, o textarea tem altura máxima de 11rem e os botões de anexos e envio ficam alinhados na mesma linha.
- A rota `/api/painel/indicadores-gestao-a-vista/horarios-corte/tabela/paginada` aceita filtros analíticos de tabela via query params `f.tabelaBusca`, `f.tabelaColuna.filial`, `f.tabelaColuna.linhaOuOperacao` e `f.tabelaColuna.saiuNoHorario`; o hook `useHorariosCorteTabelaPaginada` inclui esses filtros no `queryKey`.
- Saúde do ETL expõe `/api/painel/etl-saude/serie` para a volumetria diária de sucessos e falhas (`dataReferencia`, `qtdSucesso`, `qtdFalha`); o hook `useEtlSaudeTaxasDiarias` inclui o período no `queryKey`. O overview usa a mesma fonte `dbo.log_extracoes`: cada linha representa a execução de uma entidade/tabela, sucesso é `COMPLETO`/`SUCCESS`/`SUCESSO` e qualquer outro status (inclusive vazio) conta como falha; assim os KPIs de erro, total e taxa permanecem coerentes com a série e o resumo por tabela.
- Saúde do ETL expõe `/api/painel/etl-saude/evolucao-insercoes-atualizacoes` para a série temporal diária de inserções/persistências e atualizações idempotentes; o hook `useEtlSaudeEvolucaoInsercoesAtualizacoes` inclui o período no `queryKey`.
- Saúde do ETL expõe `/api/painel/etl-saude/tabelas/resumo` para o consolidado cross-table de auditoria (`tabelaAlvo`, `qtdExtracoes`, `qtdSucessos`, `qtdFalhas`, `totalRegistrosGravados`, `primeiraExtracao`, `ultimaExtracao`, `menorDataNegocio`, `maiorDataNegocio`); o hook `useEtlSaudeTabelasResumo` inclui o período no `queryKey`.
- Integrações mantém `PENDENCIAS` e `SUCESSO` como escopos de query para `/api/painel/integracoes`, mas no frontend esses escopos atuam como tabs de status da tabela; a aba superior `Integrações` preserva o último status selecionado ao voltar de `Quarentena`.
- O contrato de métricas de Integrações inclui `rotuloDados` e `rotuloComprovante`, fornecidos pelo Satélite por sistema destino. O Dashboard usa esses rótulos no resumo por etapa e adota os termos genéricos Dados/Eventos e Comprovantes/POD nos KPIs e cabeçalhos, preservando XML/Dados e Canhoto para PPG/Vedacit e exibindo AddEvents e POD/Comprovante para SELIA.
- A barra de Integrações possui multisseleção `Integração` (PPG, Vedacit e SELIA), persistida na URL. A seleção é enviada como parâmetro repetido `destino` para a auditoria, evolução diária, resumo legado por etapa, Quarentena e suas exportações; gráficos e KPIs só completam com métricas zeradas os destinos escolhidos. Sem seleção, o campo mostra `Todos`, os três destinos permanecem visíveis e nenhum parâmetro é enviado. O frontend descarta métricas retornadas fora da seleção e só oculta série diária/tabela quando detecta destino externo com volume real, exibindo alerta em vez de apresentar indicadores incorretos.
- A Quarentena identifica Vedacit, PPG e SELIA com estilos próprios; seu escopo é derivado dos destinos retornados pela API, com fallback apenas quando a lista estiver vazia. Catálogo da Home e descrição da permissão de Integrações usam linguagem genérica de dados, eventos e comprovantes.
- A aba Quarentena mantém a lista de pendências atuais e, abaixo dela, o `Histórico de repescagens`: tabela paginada que recebe do Satélite apenas tentativas posteriores à entrada registrada em quarentena, com destino, NF-e, etapa, data de entrada, data da repescagem, resultado e motivo sanitizado. Ambas iniciam com 10 linhas e oferecem CSV. Nessa aba, o destino é um seletor único `Todos`/`Vedacit`/`PPG`/`SELIA`, ao lado das abas principais, que substitui a multisseleção geral e filtra ambas as tabelas; o Dashboard somente faz proxy autenticado das rotas de histórico e exportação do Satélite, sem agregação em memória.
- A validação de Integrações cobre respostas SELIA com rótulos AddEvents/POD, pendência com comprovante, paginação/filtros e exportação; a validação de Quarentena cobre SELIA e sua exportação. O proxy do Dashboard mantém o corpo e os parâmetros vindos do Satélite, sem agregação em memória.
- As exportações de Integrações e Quarentena percorrem o Satélite via proxies autenticados (`/api/painel/integracoes/exportacao` e `/api/etl/quarentena/erros/exportacao`), preservam destino, período, escopo, filtros de tabela e ordenação quando aplicáveis e retornam CSV sem materializar a tabela no Dashboard. `AnalyticalDataTable` aceita `acoesCabecalho`, usado pelo `ExportButton` das abas Pendências Operacionais e Integrados com Sucesso; a Quarentena usa o slot equivalente de `DataTable`. O reprocessamento de SELIA permanece indisponível até validação do fluxo seguro no Satélite.
- `FiltroValidacaoJwt` também valida o bearer token em dispatches assíncronos. Isso mantém a autenticação das respostas `StreamingResponseBody`, como as exportações CSV, e evita que o segundo dispatch seja interpretado como sessão expirada.
- O gráfico "Resumo por Entidade de Integração" não realiza chamada HTTP própria; ele usa `metricasConsolidadas` do endpoint `/api/painel/integracoes`, separando cada sistema destino em `XML/Dados` e `Canhoto` no frontend para evitar 404 quando o Satélite em execução ainda não expõe `/api/auditoria/integracoes-clientes/resumo-tabelas`.
- O contador de Gerenciar solicitações na Home considera exclusivamente solicitações ativas com status `ABERTA`; concluídas e arquivadas não entram no número exibido.

## Regras de Negócio Consolidadas
- A sessão isola o cache frontend por identidade, papel, setor, permissões, filiais e exigência de troca de senha. Alterações nesse escopo limpam queries/mutações e remontam os consumidores; uma resposta antiga não repovoa o cache após a troca. Renovação de validade sem mudança de acesso preserva os dados. O histórico de Performance inclui parceiros logísticos na chave de cache.
- Textos externos interpolados em HTML de tooltip passam por `escapeHtml`; markup fixo e marcadores ECharts são preservados, sem mudar rótulos Canvas ou valores de filtros. Três testes reproduziram injeção de elementos antes da correção.
- O isolamento DEV combina profiles ativos e marcadores de ambiente. `SqlServerJdbcUrl` interpreta localmente propriedades escapadas e recusa banco ausente, sintaxe inválida e definições duplicadas; nomes de produção dentro de chaves não contornam mais o bloqueio. O parser não abre conexão nem registra URLs.
- Autorização usa `PermissaoResolverService.resolverAcesso`/`AcessoResolvidoDTO` para reaproveitar o papel resolvido na chamada: o fluxo comum de `authoritiesFor` passou de quatro para uma invocação ao repositório de papéis. Sessão e authorities preservam a matriz ACL, com revogação relida na próxima requisição e sem cache entre chamadas.
- `AutorizacaoQueryCountTest` simula as duas leituras da revogação com `thenAnswer` e listas tipadas, sem criar array genérico para varargs; o cenário e a contagem de duas consultas permanecem iguais.
- JSON malformado, ausente ou com tipo incompatível agora retorna HTTP 400 por `HttpMessageNotReadableException`, com mensagem genérica e sem registrar o payload do parser. Quatro casos MVC reproduziram HTTP 500 antes da correção e passaram após o tratamento.
- A validação de senha no frontend reconhece categorias Unicode: letras acentuadas contam como letras e não como símbolos. Casos de paridade com a política Java foram validados para tamanho mínimo, categorias ausentes e acentos, mantendo o contrato de 12 caracteres com maiúscula, minúscula, número e símbolo.
- O Dashboard não é dono do banco analítico; alterações em views/tabelas/fatos do ETL devem ser feitas em `etl-extracao-dados`.
- É proibido hardcode de `ETL_SISTEMA` no Java/JPA/queries do Dashboard; use nomes simples e synonyms/migrations do banco próprio quando necessário.
- Toda alteração estrutural do banco próprio deve estar em Flyway e refletida em setup/validation quando aplicável.
- O usuário de runtime deve ter privilégio mínimo; permissões DDL pertencem ao processo controlado de migration.
- Consultas de dashboard exigem período válido, `dataInicio <= dataFim` e máximo de 365 dias.
- Para colunas `DATETIMEOFFSET`, usar `PeriodoOffsetDateTimeHelper` com zona `America/Sao_Paulo`, início inclusivo e fim exclusivo.
- Agregações, somas, contagens, rankings, filtros volumosos, distinct, paginação e exportação devem ser executados no SQL Server.
- ACL: permissões efetivas combinam catálogo ativo, template do setor e overrides individuais `GRANT`/`DENY`; admins de acesso, admin plataforma e desenvolvedor têm privilégios elevados.
- Usuários administrativos são removidos apenas por inativação lógica (`ativo = false`) com revogação de refresh tokens; não existe endpoint nem serviço de exclusão definitiva de usuário.
- Rotas backend usam `@PreAuthorize("@acessoSeguranca...")`; frontend espelha permissões em `src/utils/accessControl.ts`.
- Escopo de filiais por usuário aceita `HERDAR_SETOR`, `TODAS` e `SELECIONADAS`; seleção vazia não é válida em `SELECIONADAS`.
- Parceiros logísticos não atuam como filiais emissoras nas tabelas fato; por isso a barra global operacional não exibe dropdown/input de parceiros. O frontend preserva o estado `parceirosLogisticos` e o backend segue tratando `filiais` + `parceirosLogisticos` como filtro consolidado de filial para SQL, paginação, exportação e dimensões dependentes.
- Sessão: access token JWT fica em memória no frontend; refresh token rotativo fica em cookie HttpOnly; 401 tenta refresh silencioso e encerra sessão se falhar.
- Troca obrigatória de senha: o administrador informa uma senha temporária única no modal de `POST /api/usuarios/{usuarioId}/reset-senha`, validada pela política de senha e persistida apenas como hash Argon2id. O usuário só obtém nova sessão após enviá-la junto da nova senha válida para `/api/auth/nova-senha-obrigatoria`; contas marcadas não recebem authorities no filtro JWT e não podem renovar sessão.
- Redefinição solicitada: `POST /api/auth/password-reset-request` aceita somente JSON e origem CORS permitida, aplica limite de cinco pedidos por IP a cada hora e sempre retorna a mensagem genérica de registro. Apenas contas ativas existentes recebem `password_reset_requested_at`; o campo é devolvido exclusivamente quando o solicitante da gestão de usuários é o usuário supremo. A redefinição administrativa, a troca obrigatória concluída e a alteração de senha no cadastro removem a solicitação, revogam sessões quando aplicável e mantêm `exige_troca_senha` até a senha privada ser criada.
- Os contratos Java mantêm construtores compatíveis para testes e consumidores internos: `RateLimitService` de seis parâmetros aplica os valores padrão do limite de redefinição, enquanto o construtor com propriedades Spring é explicitamente marcado com `@Autowired`; o construtor anterior de `UsuarioAcessoDTO` preenche `passwordResetRequestedAt` com `null`.
- Online administrativo significa `ultima_atividade >= DATEADD(MINUTE, -15, SYSDATETIMEOFFSET())`; a contagem e o status da listagem são calculados por SQL nativo, sem varredura de usuários ou timeout em memória na JVM.
- A lista exibida no hover de "Usuários Online Agora" é derivada de `usuariosOnlineDetalhes` do resumo de sessões e considera `ultima_atividade >= DATEADD(MINUTE, -15, SYSDATETIMEOFFSET())`; o tempo exibido é uma formatação amigável de `Date.now() - ultimaAtividade`, com fallback seguro quando o pulso não tem data válida.
- Política de senha: mínimo 12 caracteres, com maiúscula, minúscula, número e símbolo; hashes novos usam Argon2id, com upgrade de hash legado quando aplicável.
- Login bloqueia após 5 falhas por 15 minutos; troca de senha revoga todos os refresh tokens do usuário.
- Rate limit separa login, API geral, exportação e redefinição de senha; os contadores ficam compartilhados no banco `DASHBOARDS` em `acesso.rate_limit_buckets`, com lock serializável por chave, expiração indexada e sem confiar em `X-Forwarded-For` fora da origem Cloudflare validada. `/api/interno/**` exige `X-API-KEY`.
- A API Spring emite headers defensivos por `SegurancaWebConfig`: CSP restritivo para respostas da API, HSTS, `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy: no-referrer`, `Permissions-Policy` e cache-control padrão do Spring Security.
- CORS de produção aceita somente origens públicas HTTPS; origens locais em produção e mistura de origem local com pública causam falha de startup. A política de `/api/**` permite `GET`, `POST`, `PUT`, `PATCH`, `DELETE` e `OPTIONS`, incluindo o preflight das ações administrativas de solicitação na Home.
- Metas de fretes: branch `GLOBAL` é `NULL`, ano 2000-2100, mês 1-12 e meta não negativa; replicação só ocorre se destino estiver vazio e origem tiver metas.
- Metas de KPIs de Gestão à Vista: indicadores válidos são `delivery_performance`, `collector_usage`, `cargo_cubage`, `cargo_indemnity` e `cutoff_time`; metas entre 0 e 100; competência normalizada para primeiro dia do mês; override igual ao global é removido.
- Metas de custo de manifestos: branch `GLOBAL` vira `NULL`, contrato padrão `GERAL/GERAL`, `contract_type_key` e `classification_key` são canônicos em caixa alta após `trim`, `classification_key` vazia/`GERAL`/`GLOBAL` vira `NULL`, custo é arredondado para 2 casas e não pode ser negativo; filtros sem dimensão orçamentária tornam orçamento inaplicável.
- Endpoints com `@RequestBody` administrativo usam `@Valid`; DTOs de metas de fretes e manifestos validam competência, tamanhos máximos e metas não negativas na borda HTTP.
- Uploads administrativos de usuários, metas de manifestos, clientes sem cubagem e horários de corte validam extensão contratual e assinatura/conteúdo real antes de abrir planilhas: OOXML `.xlsx`, OLE2 `.xls` e CSV texto quando aplicável.
- Performance de Entrega em Gestão à Vista: o ranking/drill-down percorre `RESPONSAVEL -> REGIAO -> CIDADE`. A fonte é `dbo.fato_gestao_vista_fretes`, filtrada por `indicador_codigo = 'PE'`, `data_referencia >= :dataInicio`, `data_referencia < :dataFimExclusivo`, `is_linha_valida_indicador = 1`, `excluido_na_origem = 0` e escopo de filiais. Responsável e região selecionados viram filtros parametrizados; agrupamento, totalizações e Top 50 são calculados no SQL Server.
- Cubagem de Mercadorias em Gestão à Vista: a fonte é `dbo.fato_gestao_vista_fretes`, filtrada por `indicador_codigo = 'CB'`, janela sargable em `data_referencia`, `is_linha_valida_indicador = 1`, `is_pagador_excluido_cubagem = 0`, `excluido_na_origem = 0`, escopo de filiais e `NOT EXISTS` contra `dbo.cliente_excecao_cubagem` usando `pagador_documento_key = cliente_cnpj` e `ativo = 1`. O Java não carrega lista de CNPJs para memória; o cruzamento é indexado no SQL Server.
- Horário de Corte: viagens Raster são avaliadas pela saída real contra o horário de corte da rota com tolerância de 10 minutos; as linhas CWB-NHB e RJR-SPO usam corte operacional de 22:00h, enquanto o sentido NHB-CWB permanece em 04:00h e SPO-RJR em 23:30h; SMs com justificativa ativa em `dbo.viagem_justificativas` contam como no horário, expõem o texto da justificativa na tabela/exportação CSV e podem ter a justificativa inativada por DELETE HTTP idempotente. O banco preserva o registro com `ativo = 0`, e as queries nativas filtram `vj.ativo = 1` para retornar a SM ao status original de contabilização.
- A paginação de Horário de Corte é server-side de ponta a ponta: filtros de Filial, Linha/Operação e Status são aplicados no SQL antes da contagem e da página; `Status` aceita os rótulos `NO PRAZO`, `FORA DO PRAZO` e `SEM DADO`.
- Alteração de KPI deve atualizar `frontend/src/constants/kpiDictionary.ts`; alteração de regra/fonte de gráfico deve atualizar `frontend/src/constants/chartDictionary.ts`.
- Saúde do ETL filtra a auditoria operacional por `timestamp_inicio >= :dataInicio AND timestamp_inicio < :dataFimExclusivo`; a série de sucessos/falhas classifica `status_final` normalizado em `COMPLETO`/`SUCCESS`/`SUCESSO` como sucesso e demais status como falha, o gráfico de evolução usa `registros_extraidos` como inserções/persistências registradas e `noop_count` como atualizações idempotentes disponíveis no contrato atual do ETL, e o resumo cross-table agrupa `dbo.log_extracoes` por `entidade`, com `COUNT_BIG`, contagem de sucessos/falhas por `status_final`, `SUM(registros_extraidos + noop_count)`, `MIN(timestamp_inicio)`, `MAX(timestamp_fim)`, `MIN/MAX` das datas de negócio publicadas nas views analíticas e ordenação crescente por `total_registros_gravados`, tudo calculado no SQL Server.
- Em Saúde do ETL, os campos "Primeira" e "Última" do resumo por tabela alvo são interpretados como limites históricos dos registros retornados no período filtrado; tooltips devem manter essa distinção explícita para não serem confundidos com horário de execução ou atualização da tela.
- Em Saúde do ETL, "Dados desde" e "Dados até" representam a idade real do dado de negócio nas tabelas/views publicadas, não o horário da extração nem o `updatedAt` da tela.
- Em Integrações, o Dashboard não cria agregação JVM sobre logs do Satélite; o gráfico "Resumo por Entidade de Integração" reutiliza as métricas consolidadas já agregadas pelo Satélite em `/api/painel/integracoes`, calculando no frontend `totalSucesso = ROUND(totalRegistros * percentualEtapa / 100)` e `totalErro = totalRegistros - totalSucesso` para as etapas XML/Dados e Canhoto por sistema destino. A série principal do gráfico mostra `totalProcessado`; sucesso, erro, taxa e criticidade ficam nos KPIs laterais e no tooltip para evitar redundância visual com "Saúde por Sistema Destino".
- O proxy de Integrações delega a consulta, evolução e exportação diretamente ao Satélite; indisponibilidade de rede é devolvida como `503 Service Unavailable`, sem fallback ou consulta adicional ao ESL. Os testes do serviço asseguram a delegação e esse tratamento.
- A telemetria Vedacit SFTP é consumida exclusivamente pelo Satélite: o proxy do Dashboard expõe o resumo por cliente e o histórico paginado de ciclos, preservando início/fim, conexão, status e contagens técnicas já auditadas, sem agregar em memória, sem acessar ESL/SFTP/SOAP e sem transportar caminho, credencial ou documento fiscal.
- A linha do tempo SFTP aplica no Satélite os filtros de cliente, resultado e período, com paginação server-side fixa de 10 registros; o frontend apenas serializa os filtros e apresenta um resumo horizontal compacto por cliente, com filtros integrados ao cabeçalho do histórico. A navegação sempre informa total, tamanho da página e página atual para impedir crescimento da tabela; a tabela é ocultada quando não há ciclos no período. A próxima execução é apresentada como estimativa de 30 minutos após o término do ciclo. Os contratos, proxy e filtros foram validados por testes Java/Vitest e build TypeScript/Vite.
- A antiga aba de Quarentena e seus proxies do Dashboard foram aposentados: ela apresentava repescagens do fluxo anterior e não pertence ao monitoramento passivo atual. A página de Integrações mantém somente auditoria paginada, métricas históricas sem reenvio e telemetria do `WORK-SFTP-CLIENTES`; os botões planos e visíveis `Todos`, `PPG`, `Vedacit` e `SELIA` permitem recortar o destino e propagam a escolha para métricas, gráficos, tabela e exportação, sem criar card interno na barra. As abas de resultado e a exportação ficam no cabeçalho da própria tabela, junto da busca e dos filtros de coluna. Registros legados continuam preservados exclusivamente na auditoria do Satélite, sem exclusão física.
- Coletas por Região Logística: o nível raiz do gráfico usa a macro-região resolvida no banco por precedência Faixa de CEP -> Cidade/UF -> Cidade - UF; o drill-down de segundo nível lista as cidades daquela região logística, sempre com agrupamento e totais calculados no SQL Server.
- Produção é controlada por operador humano: não executar `iniciar-prod.bat`, não reiniciar processos e não liberar portas 5010/5173 por automação.
- Backup operacional do banco próprio é versionado em `scripts/backup-dashboard-db.ps1`, com `BACKUP DATABASE`, `CHECKSUM`, `RESTORE VERIFYONLY`, bloqueio para bancos fora de `DASHBOARDS`/`DASHBOARDS_DEV` e retenção local segura. A rotina diária pode ser registrada pelo operador com `scripts/install-dashboard-backup-task.ps1`.
- Arquivos, SQL e seeds devem permanecer em UTF-8 e sem mojibake.

## Protocolo de Planejamento de Requisições
- Antes de iniciar qualquer planejamento ou escrita de código, a IA DEVE OBRIGATORIAMENTE ler `AGENTS.md` do projeto local e `CONTEXTO_GLOBAL.md`.
- O `CONTEXTO_GLOBAL.md` dita as regras do ecossistema e o `AGENTS.md` dita as regras locais. Falhar em ler e aplicar essas regras resulta em quebra arquitetural.
- Ao receber uma nova requisição para este projeto, atuar como Arquiteto de Software e usar este `states.md` como ESTADO ATUAL.
- A análise deve respeitar a stack, a arquitetura, as fronteiras de banco e os contratos de dados descritos neste arquivo.
- A resposta de planejamento deve retornar somente o bloco `## Tarefas Pendentes`, formatado em Markdown.
- O bloco deve decompor a requisição em tarefas sequenciais, lógicas e granulares, especificando arquivos exatos, variáveis, tipagens e validações que deverão ser alterados ou criados.
- É proibido incluir saudações, conclusões, explicações fora dos bullets ou reescrever outras seções durante a resposta de planejamento.

## Tarefas Pendentes

- [ ] Tratar a dívida remanescente de cobertura: 159 classes backend sem instrução executada; ampliar comportamento/falhas por domínio, sessões concorrentes e E2E das demais páginas. Validar dialeto, planos, transações e pool em SQL Server isolado; a cobertura de 52,38% backend/23,57% frontend e os 952 testes não representam todas as combinações do projeto.
- [ ] Fechar os avisos residuais de dependências: dois pacotes moderados React Router e 20 pacotes Maven com 86 avisos (dez críticos, 30 altos, 32 moderados, 14 baixos; 85 aliases CVE). A aplicabilidade foi triada por configuração/caminhos, sem prova de exploração. Preparar migração coordenada da linha Spring Boot 3.2, já fora de suporte aberto; 3.5.16/Java 17 é candidata ainda não executada. Validar Hibernate/Flyway/Security/Jackson, startup, uploads e SQL Server em homologação. Detalhes e fontes em `docs/auditoria-aprofundada-2026-09-10.md`; não silenciar alertas por ausência de exploração nos testes.
- [ ] Medir p50/p95 SQL/HTTP, planos e pool em SQL Server isolado para as quatro páginas, conforme `docs/performance-quatro-paginas-2026-09-10.md`: sete agregações no pacote de gráficos de Faturamento, sazonalidade de Manifestos acoplada ao resumo completo, CTE/deduplicação repetida de Performance e composição executiva de múltiplos domínios com agrupamento mensal na JVM. Investigar os aumentos observados no cenário limitado (Manifestos +4,4%, Executivo +2,7%) antes de alegar aceleração nessas telas. Revisar separadamente o override de datas explícitas pelo preset de 180 dias do Executivo. Cotações/Gestão à Vista ainda mantêm timers; Coletas mantém agregações sequenciais. A inicialização temporária ECharts foi substituída e validada na correção de animações; priorização por viewport permanece uma hipótese. Em Localização, medir as quatro agregações sequenciais de buscarDashboardConsultaUnica e o detalhamento separado antes de atribuir a demora restante ao navegador. Não aumentar concorrência SQL nem alterar estruturas do ETL sem plano/custo e paridade medidos.

- [ ] Publicação pelo operador humano: os candidatos atuais do Dashboard são `frontend/.tmp/quality-build/20260910-animacao-final/dist` e `backend/target/quality/20260910-103509-123598/dashboard-api-1.0.0.jar`, incluindo Integrações, ECharts modular atualizado, fila de Performance, cancelamento, isolamento do cache, escape de tooltips, validação DEV robusta, resolução de papel por chamada e correções anteriores de senha/JSON. Incluem também a fila das quatro páginas, cancelamento adicional e consultas evitadas no Executivo/Manifestos. Incluem a correção compartilhada de inicialização dos gráficos, memoização das cinco páginas afetadas e carregamento correto da conversão em Cotações. Validação atual: 687 testes backend/265 frontend, TypeScript, lint, encoding e builds; as pendências de segurança por versão permanecem explicitadas acima. O candidato Satélite permanece `satelite-tms-api/target/unit-tests/coverage-20260909-235105-562/satelite-0.0.1-SNAPSHOT.jar`, com os 27 testes da validação anterior, não reexecutados nesta rodada. A publicação não acrescenta auditoria de outros processos ESL, lacuna em `docs/integracoes-origem-comprovantes-2026-09-09.md`. A verificação real das imagens está em `docs/integracoes-canhotos-2026-09-10.md`. Os candidatos anteriores e todos os artefatos operacionais foram preservados; runtime e portas 5010/5173 continuam exclusivos do operador humano.
